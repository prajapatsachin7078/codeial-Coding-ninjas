<link rel="stylesheet" href="/css/user_profile.css">
<div class="container">
    <div class="row">
        <br>
            <% if(user.id == profile_user.id) { %>
                <!-- if user matches show the form show the form  -->
                <form action="/users/update/<%= profile_user.id %>" method="post">
                    <input type="text"  name="name" placeholder="Your Name" value="<%= profile_user.name %>" required>
                    <input type="text"  name="email" placeholder="Your Email" value="<%= profile_user.email %>" required>
                    <input type="submit" value="Update">
                </form>

            <% }else{ %>
                <!-- else show the profile  -->
                <div class="col-md-6 offset-md-3 bg-primary text-light p-3 rounded">

                    <h1>User Profile</h1>
                    <p class="mb-0">Name: <%= profile_user.name %></p>
                    <br>
                    <p>Email: <%= profile_user.email %></p>
                </div>
            <% } %>
    </div>
</div>
